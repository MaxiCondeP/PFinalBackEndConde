import{firebaseMessageContainer} from '../containers/firebaseMessageContainer.js'

export default class firebaseMessageDao extends firebaseMessageContainer{
    constructor(){
        super();
    }
}